services:
  - type: web
    name: first-edition-atelier
    env: node
    plan: free
    buildCommand: cd atelier && npm install && npm run build
    startCommand: cd atelier && node dist/server.cjs
    envVars:
      - key: NODE_ENV
        value: production
      - key: GEMINI_API_KEY
        sync: false
